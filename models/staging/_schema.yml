version: 2

sources:
  - name: external__jaffle_shop
    database: training-dbt-421010 # avisia-training-dbt
    schema: dbt_demo
    tables:
      - name: customers
      - name: orders

models:
  - name: stg_customers
    description: "Staging view to retrive customers information."
    columns:
      - name: customer_id
        tests:
          - not_null


